//archivo donde esta linkeado este script: ../ES6.html

/* 

En javascript casi todo es un objeto 

Booleans can be objects (if defined with the new keyword)
Numbers can be objects (if defined with the new keyword)
Strings can be objects (if defined with the new keyword)
Dates are always objects
Maths are always objects
Regular expressions are always objects
Arrays are always objects
Functions are always objects
Objects are always objects


Javascript Primitives 

Un valor primitivo es un valor que no contiene propiedades ni metodos.

Un dato primitivo es un dato que contiene un valor primitivo 

JavaScript defines 5 types of primitive data types:

string
number
boolean
null
undefined

Los valores primitivos son inmutables 

if x = 3.14, you can change the value of x. But you cannot change the value of 3.14.

Los objetos son variables 

Example: 

var person = "Uriel"

Los objetos son variables pero pueden contener many values.

Los valores son escritos como name: value dentro de parentesis y separados con una coma

Example: 

var person {
    name: "uriel",
    age: 18
}

Un objeto en javascript es una coleccion de valores nombrados

Los valores nombrados son conocidos como propiedades.

Example: 

Objeto: persona
Propiedad: name - Valor: "uriel"
Propiedad age - Valor: 18


dato curioso

Objects written as name value pairs are similar to:

Associative arrays in PHP
Dictionaries in Python
Hash tables in C
Hash maps in Java
Hashes in Ruby and Per

Metodos de un objeto

Los metodos son acciones que puede realizar un objeto 
*/